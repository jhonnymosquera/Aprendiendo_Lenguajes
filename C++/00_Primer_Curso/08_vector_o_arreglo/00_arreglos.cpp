

/**
 * definimos el tipo de dato que almacerana el array
 * le damos el nombre al array
 * luego definimos la cantidad maxima de elementos en el array
 */
int variable[10];

/**
 * definimos el tipo de dato que almacenara el array
 * le damos nombre al array
 * si esta vacio podemos agregar cualquier cantidad de elementos en el array
 */
int numeros[] = {1, 4, 6, 7, 3};
char letra[] = {'a', 'e', 'i', 'o', 'u'};